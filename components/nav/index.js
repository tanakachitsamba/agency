import { h } from 'preact'
import { Nav, NavContainer, Menu, Logo } from './styles'

export default () => (
    <Nav>
        <NavContainer>
            <Logo>Working Title.</Logo>
            <Menu />
        </NavContainer>
    </Nav>
)
