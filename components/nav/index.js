import React from 'react'
import { Nav, NavContainer, Menu, Logo } from './styles'

export default () => (
    <Nav>
        <NavContainer>
            <Logo>Working Title.</Logo>
            <Menu />
        </NavContainer>
    </Nav>
)
